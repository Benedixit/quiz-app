export type useQueryParams = {
    onChange: (path: string) => void;
    pathname: string;
    searchParams: string;
};
export declare function useQueryParams<T>({ onChange, pathname, searchParams, }: useQueryParams): {
    setQueryParams: (params: Partial<T>) => void;
};
