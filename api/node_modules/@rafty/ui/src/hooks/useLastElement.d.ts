import type { FetchNextPageOptions, InfiniteQueryObserverResult } from "@tanstack/react-query";
export type useLastElementProps<T, U = unknown> = {
    isFetching?: boolean;
    hasNextPage?: boolean;
    fetchNextPage: (options?: FetchNextPageOptions) => Promise<InfiniteQueryObserverResult<T, U>>;
};
export declare function useLastElement<T, U = unknown>({ isFetching, hasNextPage, fetchNextPage, }: useLastElementProps<T, U>): (node: HTMLDivElement) => void;
