import type { AvatarGroupContext } from "./context";
/**
 * Calculate the max width for AvatarGroup
 */
export declare function calculateWidth(size: AvatarGroupContext["size"], length: number): number;
