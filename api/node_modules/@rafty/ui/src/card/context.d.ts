/// <reference types="react" />
import { type SizeType } from "../utils";
export declare const CardProvider: import("react").Provider<CardContext>, useCardContext: () => CardContext;
export interface CardContext {
    readonly size: SizeType;
    readonly variant: "outline" | "elevated";
    readonly isUnstyled: boolean;
    readonly has: {
        header: boolean;
        footer: boolean;
    };
}
