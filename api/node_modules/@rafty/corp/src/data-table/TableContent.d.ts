import { type Table } from "@tanstack/react-table";
export type TableContent<T> = {
    table: Table<T>;
    colSpan: number;
    isLoading: boolean;
};
export declare function TableContent<T>({ table, colSpan, isLoading, }: TableContent<T>): import("react/jsx-runtime").JSX.Element;
