import { type SizeType } from "@rafty/ui";
import { type ColumnDef, type ColumnSizingState, type OnChangeFn, type RowSelectionState, type SortingState } from "@tanstack/react-table";
import type { HTMLAttributes, ReactNode } from "react";
export type ColumnType<T> = ColumnDef<T>;
export type DataTable<T> = {
    columns: ColumnType<T>[];
    data?: T[];
    enableRowSelection?: boolean;
    enableColumnsSorting?: boolean;
    isLoading?: boolean;
    isError?: boolean;
    enableColumnResizing?: boolean;
    size?: SizeType;
    onSortingChange?: (value: SortingState) => void;
    onColumnSizingChange?: (value: ColumnSizingState) => void;
    onRowsSelectedChange?: OnChangeFn<RowSelectionState>;
    notFoundMessage?: ReactNode;
    errorMessage?: ReactNode;
    rowsSelected?: RowSelectionState;
    className?: HTMLAttributes<HTMLDivElement>["className"];
};
export declare function DataTable<T>({ columns, data, isLoading, isError, enableRowSelection, enableColumnResizing, enableColumnsSorting, size, notFoundMessage, errorMessage, onRowsSelectedChange, rowsSelected, className, ...props }: DataTable<T>): import("react/jsx-runtime").JSX.Element;
