import type { ThreadType } from "../types";
export type Loom<T extends Record<string, unknown>> = {
    id?: string;
    blueprint: ThreadType<T>;
};
export declare function Loom<T extends Record<string, unknown>>({ id, blueprint, }: Loom<T>): import("react/jsx-runtime").JSX.Element;
