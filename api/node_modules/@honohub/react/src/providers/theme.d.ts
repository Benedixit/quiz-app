import { type PropsWithChildren } from "react";
export declare enum ColorMode {
    DARK = "dark",
    LIGHT = "light",
    SYSTEM = "system"
}
export type ThemeContextType = ReturnType<typeof useThemeWatcher>;
export type ThemeProvider = PropsWithChildren;
export declare const ThemeProvider: ({ children }: ThemeProvider) => import("react/jsx-runtime").JSX.Element;
declare function useThemeWatcher(): {
    theme: ColorMode;
    setTheme: (newTheme: ColorMode) => void;
};
export declare const useTheme: () => {
    theme: ColorMode;
    setTheme: (newTheme: ColorMode) => void;
};
export {};
