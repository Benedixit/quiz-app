import { type ColumnType } from "@rafty/corp";
import type { CollectionType } from "../types";
export type DataTable<T> = {
    columns: ColumnType<T>[];
} & Pick<CollectionType, "slug" | "actions">;
export declare function DataTable<T = unknown>({ columns, slug, actions, }: DataTable<T>): import("react/jsx-runtime").JSX.Element;
